# 国内地址
```bash
curl -LSs https://gitee.com/muaimingjun/key/raw/main/huawei-matebook.pub -o id_rsa.pub && cat id_rsa.pub >> ~/.ssh/authorized_keys && rm id_rsa.pub
```
# 国外地址
```bash
curl -LSs https://github.com/hyh1750522171/key/raw/main/huawei-matebook.pub -o id_rsa.pub && cat id_rsa.pub >> ~/.ssh/authorized_keys && rm id_rsa.pub
```